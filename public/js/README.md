# Deprecated vanilla stubs

`chain.js` and `privy.js` here are **not** used by the Vite React app.

Real modules live at:
- `src/chain/` — Starknet contract + settle ABI
- `src/privy/` — Privy provider + session

Do not edit these for production rails.
